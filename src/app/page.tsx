// Placeholder. The real landing page, signup, and login arrive with #4.
export default function Home() {
  return (
    <main className="mx-auto flex min-h-screen max-w-2xl flex-col justify-center gap-4 px-6">
      <h1 className="text-3xl font-semibold tracking-tight">Jobfinder</h1>
      <p className="text-base text-gray-600">
        Fetches job openings on a schedule, filters them against your stated
        criteria, and presents the matches for review.
      </p>
    </main>
  );
}
